# Contributing

```sh
make install   # uv sync
make test      # uv run pytest
make lint      # uv run ruff check .
make format    # uv run ruff format .
```

Every change must include tests. Before marking a phase step done, either confirm existing coverage is sufficient or write new tests first.
